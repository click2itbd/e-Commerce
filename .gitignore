node_modules/
build/
# dist/ is intentionally tracked — cPanel reads it for deployment
# Only ignore backend's compiled output during local dev (it's rebuilt by .cpanel.yml)
backend/dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.env*.local
firebase-applet-config.json
backend/.env
backend/.env.local
firebase-service-account.json
backend/firebase-service-account.json
*.pem
*.key
