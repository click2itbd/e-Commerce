const fs = require('fs'); let c = fs.readFileSync('src/pages/admin/tabs/purchase/VendorDueList.tsx', 'utf8'); c = c.replace(/CustomerDueList/g, 'VendorDueList').replace(/Customer/g, 'Vendor').replace(/customers/g, 'vendors').replace(/setCustomers/g, 'setVendors').replace(/selectedCustomer/g, 'selectedVendor').replace(/historyCustomer/g, 'historyVendor').replace(/dueCustomers/g, 'dueVendors').replace(/entityType === 'customer'/g, 'entityType === \'vendor\'').replace(/t\.type === 'sale'/g, 't.type === \'purchase\'').replace(/t\.type === 'payment_received'/g, 't.type === \'payment_made\'').replace(/handleReceivePayment/g, 'handleMakePayment').replace(/Receive Payment/g, 'Make Payment').replace(/Payment Received/g, 'Payment Made').replace(/customer/g, 'vendor'); fs.writeFileSync('src/pages/admin/tabs/purchase/VendorDueList.tsx', c);
